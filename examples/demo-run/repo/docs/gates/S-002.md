# Gates S-002 - Assert /health Content Type

Frozen before implementation at `2026-06-22 12:35 Africa/Tunis`.

## Acceptance Gates

| Gate ID | Requirement | Verification command / method | Pass condition |
| --- | --- | --- | --- |
| `G-001` | `/health` test asserts JSON content type. | `node --test` | A test checks the `content-type` header includes `application/json`. |
| `G-002` | Existing `/health` response behavior remains unchanged. | `node --test` | S-001 health endpoint tests still pass. |
| `G-003` | No production behavior changes unless the new test exposes a real defect. | `git diff` | Production diff is empty or justified in `docs/HANDOFF.md`. |

## Required Commands

| Command | Required for PASS? | Notes |
| --- | --- | --- |
| `node --test` | yes | Must exit 0. |
| `git diff -- src test docs` | yes | Inspect changed files and scope. |

## Out Of Scope

- New endpoints.
- Monitoring integration.
- Dependency changes.

## Freeze Rule

Worker edits to this file or `S-002.sha256` fail the slice. Only Fable may
review a revision and explicitly re-freeze before workers resume.

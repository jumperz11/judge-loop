# Gates S-001 - Add GET /health

Frozen before implementation at `2026-06-22 12:20 Africa/Tunis`.

## Acceptance Gates

| Gate ID | Requirement | Verification command / method | Pass condition |
| --- | --- | --- | --- |
| `G-001` | `GET /health` returns HTTP 200. | `node --test` | Test named `GET /health returns 200` passes. |
| `G-002` | `/health` body matches frozen shape. | `node --test` | Body has exactly `status: "ok"` and integer `uptime_s`. |
| `G-003` | `GET /` behavior remains unchanged. | `node --test` | Test named `GET / still returns ok` passes. |

## Required Commands

| Command | Required for PASS? | Notes |
| --- | --- | --- |
| `node --test` | yes | Must exit 0. |

## Out Of Scope

- Auth.
- Metrics libraries.
- Deployment.
- Changing `/`.

## Freeze Rule

Worker edits to this file or `S-001.sha256` fail the slice. Only Fable may
review a revision and explicitly re-freeze before workers resume.
